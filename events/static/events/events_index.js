const oneTimeEventsList = new DataTable('#one-time-events-table', {
    responsive: true,
    searching: true
})

const trainingsList = new DataTable('#trainings-table', {
    responsive: true,
    searching: true
})